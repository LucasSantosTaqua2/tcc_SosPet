@model HomeViewModel;

@{
    ViewData["Title"] = "Postagens";
}

<link rel="stylesheet" href="~/css/user_posts_page.css" />

<h1>Minhas Postagens</h1>

<div class="home-content">
    <h3 style="text-align: center">Meus animais para adoção.</h3>
    <div class="coluna-post row row-cols-4 ">

     @foreach (var item in Model.listAdocao)
     {
            <div class="row">
                <div class="card ">
                    <img src="~/img/adocao/@Html.DisplayFor(modelItem => item.Imagem)" class="card-img-top">
                    <div class="card-body">
                        <h5 class="card-title text-center">@Html.DisplayFor(modelItem => item.Nome)</h5>
                        <p class="card-text">
                            <span>Raça:</span> @Html.DisplayFor(modelItem => item.Raca)<br>
                            <span>Sexo:</span> @Html.DisplayFor(modelItem => item.Sexo)<br>
                            <span>Idade:</span> @Html.DisplayFor(modelItem => item.Idade)<br>
                            <span>Cor:</span> @Html.DisplayFor(modelItem => item.Cor)<br>
                            <span>Porte:</span> @Html.DisplayFor(modelItem => item.Porte)<br>
                            <span>Cidade:</span> @Html.DisplayFor(modelItem => item.Cidade)
                        </p>
                    </div>
                    <div class="btn-group" role="group">
                        <a role="button" asp-route-id="@item.Id" asp-controller="Adocao" asp-action="Adotado" class="btn btn-primary">Adotado</a>
                        <a role="button" class="btn btn-danger" asp-route-id="@item.Id" asp-controller="Adocao" asp-action="Excluir">Excluir</a>
                    </div>
                </div>

            </div>
             
     }
            
    </div>

</div>
<!-------------------------------------------------------------------------------------------------->
<div class="home-content">
    <h3 style="text-align: center">Meus animais encontrados.</h3>
    <div class="coluna-post row row-cols-4 ">
        @foreach (var item in Model.listEncontrados)
        {
            <div class="row">

                <div class="card ">
                    <img src="~/img/encontrados/@Html.DisplayFor(modelItem => item.Imagem)" class="card-img-top">
                    <div class="card-body">
                        <h5 class="card-title text-center">Encontrado</h5>
                        <p class="card-text">
                            <span>Cidade:</span> @Html.DisplayFor(modelItem => item.Cidade)<br>
                            <span>Descrição:</span> @Html.DisplayFor(modelItem => item.Descricao)
                        </p>
                    </div>
                    <div class="btn-group" role="group">
                        <a role="button" class="btn btn-primary" asp-route-id="@item.Id" asp-controller="Encontrados" asp-action="Devolvido">Devolvido</a>
                        <a role="button" class="btn btn-danger" asp-route-id="@item.Id" asp-controller="Encontrados" asp-action="Excluir">Excluir</a>
                    </div>
                </div>

            </div>
        }
    </div>

</div>
<!-------------------------------------------------------------------------------------------------->
<div class="home-content">
    <h3 style="text-align: center">Meus animais desaparecidos.</h3>
    <div class="coluna-post row row-cols-4 ">
        @foreach (var item in Model.listDesaparecidos)
        {
            <div class="row">

                <div class="card ">
                    <img src="~/img/desaparecidos/@Html.DisplayFor(modelItem => item.Imagem)" class="card-img-top">
                    <div class="card-body">
                        <h5 class="card-title text-center">@Html.DisplayFor(modelItem => item.Nome)</h5>
                        <p class="card-text">
                            <span>Cidade:</span> @Html.DisplayFor(modelItem => item.Cidade)<br>
                            <span>Descrição:</span> @Html.DisplayFor(modelItem => item.Descricao)
                        </p>
                    </div>
                    <div class="btn-group" role="group">
                        <a role="button" class="btn btn-primary" asp-route-id="@item.Id" asp-controller="Desaparecidos" asp-action="Encontrado">Encontrei</a>
                        <a role="button" class="btn btn-danger" asp-route-id="@item.Id" asp-controller="Desaparecidos" asp-action="Excluir">Excluir</a>
                    </div>
                </div>

            </div>
        }
    </div>

</div>